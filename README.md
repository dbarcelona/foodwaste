# Food Recovery Oakland

A working website and business-plan draft for a B Corp-oriented food recovery startup concept.

The concept starts in Oakland, then expands to San Francisco. It focuses on recovering safe surplus food from corporate events, catered office meals, and large catering orders, then redistributing that food through local partner organizations.

## Site pages

- `index.html`: landing page and core value proposition
- `story.html`: polished founder journey
- `companies.html`: page for companies ordering catering
- `caterers.html`: page for caterers and event teams
- `resources.html`: Bay Area food recovery and food access directory
- `plan.html`: working business plan and validation questions
- `styles.css`: shared styling

## GitHub Pages

This is a static site, so it can be published directly with GitHub Pages:

1. Push these files to the `main` branch of the GitHub repository.
2. In GitHub, open Settings > Pages.
3. Set the source to deploy from the `main` branch and root folder.
4. Save and wait for GitHub Pages to publish the site.

## Current status

The local terminal in this environment does not currently recognize `git` or `gh`, so pushing to GitHub needs one of these access paths:

- install/configure Git locally and authenticate to GitHub;
- clone the repository through GitHub Desktop, then copy these files into the repo;
- upload the files directly in the GitHub web interface;
- provide a working local checkout of `dbarcelona/foodwaste` in this workspace.
